<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des utilisateurs inscrits</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950 font-sans text-gray-100 flex items-center justify-center min-h-screen">
    <div class="max-w-xl w-full p-4">
        <h1 class="text-3xl font-bold text-center mb-8">Liste des utilisateurs inscrits</h1>
        <div class="bg-zinc-900 ring-1 ring-zinc-950/5 p-6 rounded-lg shadow-lg mb-8 ring-white/10">
            <h2 class="text-2xl font-bold mb-4">Utilisateurs inscrits</h2>
            <table class="table-auto w-full text-center">
                <thead>
                    <tr>
                        <th class="px-4 py-2">ID</th>
                        <th class="px-4 py-2">Nom d'utilisateur</th>
                    </tr>
                </thead>
                <tbody>
                    {% if users is empty %}
                        <tr>
                            <td colspan="2" class="text-center py-4">Aucun utilisateur inscrit.</td>
                        </tr>
                    {% else %}
                        {% for user in users %}
                            <tr class="hover:bg-gray-700 transition duration-200 border-b-[1px] border-gray-800">
                                <td class="px-4 py-2">{{ user.id_user }}</td>
                                <td class="px-4 py-2">{{ user.username }}</td>
                            </tr>
                        {% endfor %}
                    {% endif %}
                </tbody>
            </table>
        </div>

        <h1 class="text-4xl font-bold text-center mb-8">Gestion des utilisateurs</h1>
    <div class="flex justify-center space-x-4 mb-4">
        <div class="bg-zinc-900 ring-1 ring-zinc-950/5 p-6 rounded-lg shadow-lg w-1/2">
            <h2 class="text-2xl font-bold mb-4">Inscription</h2>
            <form action="./register.php" method="POST" class="space-y-6">
                <div class="mb-4">
                    <label for="new_username" class="block text-sm font-bold">Nom d'utilisateur</label>
                    <input type="text" id="new_username" name="username" class="w-full rounded-lg px-4 py-2 text-base text-gray-100 placeholder:text-zinc-500 border border-zinc-950 bg-gray-700 focus:outline-none focus:border-blue-500 " required>
                </div>
                <div class="mb-4">
                    <label for="new_password" class="block text-sm font-bold">Mot de passe</label>
                    <input type="password" id="new_password" name="password" class="w-full rounded-lg px-4 py-2 text-base text-gray-100 placeholder:text-zinc-500 border border-zinc-950 bg-gray-700 focus:outline-none focus:border-blue-500" required>
                </div>
                <div class="mb-4">
                    <label for="confirm_password" class="block text-sm font-bold">Confirmez le mot de passe</label>
                    <input type="password" id="confirm_password" name="confirm_password" class="w-full rounded-lg px-4 py-2 text-base text-gray-100 placeholder:text-zinc-500 border border-zinc-950 bg-gray-700 focus:outline-none focus:border-blue-500" required>
                </div>
                <button type="submit" class="w-full bg-black p-3 rounded-full text-gray-100 font-bold ">S'inscrire</button>
            </form>
        </div>

        <div class="bg-zinc-900 ring-1 ring-zinc-950/5 p-6 rounded-lg shadow-lg w-1/2">
            <h2 class="text-2xl font-bold mb-4">Connexion</h2>
            <form action="./login.php" method="POST" class="space-y-6">
                <div class="mb-4">
                    <label for="username" class="block text-sm font-bold">Nom d'utilisateur</label>
                    <input type="text" id="username" name="username" class="w-full rounded-lg px-4 py-2 text-base text-gray-100 placeholder:text-zinc-500 border border-zinc-950 bg-gray-700 focus:outline-none focus:border-blue-500" required>
                </div>
                <div class="mb-4">
                    <label for="password" class="block text-sm font-bold">Mot de passe</label>
                    <input type="password" id="password" name="password" class="w-full rounded-lg px-4 py-2 text-base text-gray-100 placeholder:text-zinc-500 border border-zinc-950 bg-gray-700 focus:outline-none focus:border-blue-500" required>
                </div>
                <button type="submit" class="w-full bg-black p-3 rounded-full text-gray-100 font-bold">Se connecter</button>
            </form>
        </div>
    </div>
</body>
</html>